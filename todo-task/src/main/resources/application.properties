# Arquivo de configura��o de conex�o entre Hibernate (o app Spring) e a base de dados MySQL
spring.datasource.url = jdbc:mysql://localhost:3306/Strider?useSSL=false
spring.datasource.username = root
spring.datasource.password = 123456

# Propriedades do Hibernate
# defini��o do dialet (MySQL5)
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQL5Dialect

# configura��o do modo de inicializa��o entre modelo e BD
# update -> atualiza o schema sempre que conecta, sem criar um novo
# qualquer mudan�a no modelo tamb�m gera uma atualiza��o na tabela
spring.jpa.hibernate.ddl-auto = update

# Configura��o de porta do servidor
server.port: 8081